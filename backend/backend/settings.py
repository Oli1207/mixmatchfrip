# Ce fichier est conservé pour compatibilité mais n'est plus utilisé.
# Les paramètres Django se trouvent maintenant dans :
#   backend/settings/base.py         — paramètres communs
#   backend/settings/development.py  — développement local
#   backend/settings/production.py   — production
#
# manage.py   pointe sur → backend.settings.development
# wsgi.py     pointe sur → backend.settings.production
# asgi.py     pointe sur → backend.settings.production
